import groovy.xml.MarkupBuilder
StringWriter writer = new StringWriter()
MarkupBuilder home = new MarkupBuilder(writer)


home.html {
    head {
        title("CITE Collections service")
        link(type : "text/css", rel : "stylesheet", href : "cts.css", title : "CSS stylesheet")
        p('class' : "menu", "CITE")
    }
    body {
        div(id : "header") 
        div {
            h1("CITE Collections interface to data in Google Fusion Tables")
            p("Supply your own home page here.")
            p("Links: ")
            p("Help/how-to-configure:  TO BE ADDED")
            p {
                mkp.yield "The standard "
                a (href : "api", "CITE API")
            }
            p ("Requests:")
            ul {
                li("GetCapabilities")
                li("GetObject")
                li("OrderedBy")
                li("GetFirst")
                li("GetLast")
                li("GetNext")
                li("GetPrev")
                li("GetValidIds")
                li("GetCollectionSize")
            }
        }
    }
}

println writer.toString()
